
**An easy way to start a Docker container with the project database**

All you need is to have Docker and Docker-Compose installed. Then, simply run the command:

```bash
docker-compose up -d
```

This will create a container with a postgres database.

